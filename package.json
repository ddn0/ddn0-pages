{
  "name": "polymer-starter-kit",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-google": "^0.7.1",
    "eslint-plugin-html": "^1.7.0"
  },
  "scripts": {
    "lint": "eslint . --ext js,html --ignore-path .gitignore --ignore-path .eslintignore",
    "test": "npm run lint && polymer test"
  },
  "dependencies": {
    "bower": "^1.8.8",
    "polymer-cli": "^1.9.9"
  }
}
